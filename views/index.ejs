<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome</title>
  </head>
  <body>
    <% if(success) {%>
    <h1><%=success[0] %></h1>
    <% } %> <% if(currentUser) {%>
    <h1>Welcome <%=currentUser.username %> <a href="/logout">Logout</a></h1>
    <% } else { %>
    <h1>
      Please <a href="/login">Login</a> or <a href="/register">Register</a> to
      see more details
    </h1>
    <% } %>
  </body>
</html>
